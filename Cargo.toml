[workspace]
members = [
    "mizn-common",
    "miznd",
    "mizn-ui",
    "xtask",
]
exclude = ["mizn-ebpf"]
resolver = "2"

[profile.dev]
opt-level = 3
debug = false
panic = "abort"
lto = true

[profile.release]
opt-level = 3
panic = "abort"
lto = true
codegen-units = 1
